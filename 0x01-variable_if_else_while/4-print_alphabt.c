#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	char alphabt;

	for (alphabt = 'a'; alhabt <= 'z'; c++)
	{
	if (alphabt != 'q' && alpabt != 'e')
		putchar(alphabt);
	}
		putchar('\n');

return (0);
}
